# Acumen Logic Model 2 — Environment Variables

# PostgreSQL (Supabase)
DATABASE_URL=postgresql://postgres.[REF]:[PASSWORD]@aws-1-eu-west-2.pooler.supabase.com:6543/postgres

# JWT Secret (generate a random 64-char string for production)
JWT_SECRET=your-jwt-secret-here-change-this-to-something-random-and-long

# Admin Secret (used once to promote your account to admin)
ADMIN_SECRET=choose-a-secret-phrase-here

# Google OAuth (get from Google Cloud Console — leave as PLACEHOLDER to disable)
GOOGLE_CLIENT_ID=PLACEHOLDER

# Server
PORT=10000
NODE_ENV=production

# Allowed Origins (comma-separated — your Render URL)
ALLOWED_ORIGINS=https://your-app-name.onrender.com

# Kit (ConvertKit) Email Integration (optional — leave blank to disable)
KIT_API_KEY=
KIT_FORM_ID=
